[project]
name = "personalhub-docs"
version = "0.1.0"
description = "Documentation repo for the PersonalHub project."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.5",
]
